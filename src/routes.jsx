import React from "react";
import MainPage from "./Pages/MainPage/MainPage";

const routes = [
    { path: '/', element: <MainPage /> }
    // { path: '/preview', element: <MainPage /> }
];

export default routes;